[phases.setup]
nixPkgs = ["dotnet-sdk_10_0"]

[phases.install]
cmds = ["dotnet restore JobTrackerApi.csproj"]

[phases.build]
cmds = ["dotnet publish JobTrackerApi.csproj -c Release -o out"]

[start]
cmd = "dotnet out/JobTrackerApi.dll"